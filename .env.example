VITE_API_BASE_URL=http://localhost:8000
# Optional. Needed only for MapTiler Terrain RGB (Mapa → Relieve externo).
# Restart the Vite dev server after changing this. Never paste a key in the UI.
VITE_MAPTILER_KEY=
